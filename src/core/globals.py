# user settings

default_settings = {
    "music": 50,
    "sfx": 30,
    "fullscreen": False,
    "vsync": True,
    "screenshake": True,
    "cooked": False  # If this is loaded then assume rest is not cooked
}

settings = default_settings.copy()


# dev settings

time_dilation = 1.0
show_hitboxes = False
